{
  "name": "@keyward/platform-web",
  "version": "0.0.1",
  "description": "Cross-platform user-scoped secure storage for the web",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.0"
  },
  "dependencies": {
    "@keyward/core": "workspace:*",
    "idb": "^8.0.0"
  }
}
